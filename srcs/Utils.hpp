#ifndef UTILS_HPP
# define UTILS_HPP

# include <string>
# include <vector>
# include <sstream>

using namespace std;

vector<string> split(string str, string delim);
string toString(size_t num);

#endif
